# Test-only dependencies — not included in the production Docker image.
# Install with: pip install -r requirements.txt -r requirements-test.txt
-r requirements.txt

# ── Test framework ─────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-timeout>=2.3.0,<3.0.0

# ── HTTP mocking & test client ─────────────────────────────────────────────────
httpx>=0.27.0,<1.0.0              # async HTTP client for FastAPI test client
respx>=0.21.0,<1.0.0              # httpx-level mock for OpenAI/Gemini HTTP calls

# ── Real service containers ────────────────────────────────────────────────────
testcontainers[postgres,redis]>=4.0.0,<5.0.0  # real containers in regression tests

# ── RAG evaluation ─────────────────────────────────────────────────────────────
ragas>=0.1.0,<1.0.0               # RAG evaluation metrics (faithfulness, relevancy, etc.)
datasets>=2.0.0,<4.0.0            # HuggingFace datasets (required by RAGAS)

# ── Load testing ──────────────────────────────────────────────────────────────
locust>=2.0.0,<3.0.0
