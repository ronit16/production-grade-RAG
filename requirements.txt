# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
gunicorn>=22.0.0,<24.0.0          # production process manager; spawns uvicorn workers
python-multipart>=0.0.9,<1.0.0    # required for FastAPI file upload (UploadFile)

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0,<3.0.0
asyncpg>=0.29.0,<1.0.0            # async PostgreSQL driver
psycopg2-binary>=2.9.0,<3.0.0    # sync driver (used by Celery workers + Alembic)
alembic>=1.13.0,<2.0.0            # DB migrations

# ── Cache ──────────────────────────────────────────────────────────────────────
redis[asyncio]>=5.0.0,<6.0.0      # async Redis client

# ── Config & validation ────────────────────────────────────────────────────────
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
PyYAML>=6.0.0,<7.0.0             # prompt config loader (config/prompts.yaml)

# ── Auth ───────────────────────────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0               # RS256 JWT encode/decode
bcrypt>=4.0.0,<5.0.0              # password hashing
email-validator>=2.1.0,<3.0.0    # pydantic EmailStr validation

# ── LLM & embeddings ──────────────────────────────────────────────────────────
openai>=1.30.0,<2.0.0             # embeddings + GPT-4o
google-generativeai>=0.7.0,<1.0.0 # Gemini models (used by LiteLLM)
litellm>=1.40.0,<2.0.0            # provider-agnostic LLM routing + fallback

# ── Vector database (dense + sparse + hybrid fusion) ──────────────────────────
qdrant-client>=1.9.0,<2.0.0      # Qdrant async/sync Python client
fastembed>=0.3.0,<1.0.0           # BM25 sparse embeddings + cross-encoder reranker (ONNX)

# ── Document parsing ───────────────────────────────────────────────────────────
unstructured[all-docs]>=0.14.0,<1.0.0  # PDF, DOCX, HTML, MD parsing
pdfminer.six>=20221105
docx2txt>=0.8
markdown>=3.6.0,<4.0.0
beautifulsoup4>=4.12.0,<5.0.0

# ── Object storage ────────────────────────────────────────────────────────────
boto3>=1.34.0,<2.0.0              # S3 upload/download

# ── Async task queue ──────────────────────────────────────────────────────────
celery[rabbitmq]>=5.4.0,<6.0.0   # async worker

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator>=5.0.0,<9.0.0   # /metrics endpoint
python-json-logger>=2.0.0,<3.0.0                 # structured JSON log output
