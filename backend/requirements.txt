# ── Web framework ──────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
gunicorn                  # production process manager; spawns uvicorn workers via exec (fork-safe)
python-multipart          # required for FastAPI file upload (UploadFile)

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]
asyncpg                   # async PostgreSQL driver
psycopg2-binary           # sync driver (used by Celery workers)
alembic                   # DB migrations

# ── Cache ──────────────────────────────────────────────────────────────────────
redis[asyncio]            # async Redis client

# ── Config & validation ────────────────────────────────────────────────────────
pydantic
pydantic-settings
python-dotenv

# ── Auth ───────────────────────────────────────────────────────────────────────
PyJWT                     # RS256 JWT encode/decode
bcrypt                    # password hashing
email-validator           # pydantic EmailStr validation

# ── LLM & embeddings ──────────────────────────────────────────────────────────
openai                    # embeddings + GPT-4o
google-generativeai       # Gemini models (used by LiteLLM)
litellm                   # provider-agnostic LLM routing + fallback

# ── Vector database (dense + sparse + hybrid fusion) ──────────────────────────
qdrant-client             # Qdrant async/sync Python client
fastembed                 # BM25 sparse embeddings (Qdrant/bm25 model)

# ── Reranking ─────────────────────────────────────────────────────────────────
# sentence-transformers removed — reranker replaced with fastembed TextCrossEncoder (ONNX, no PyTorch/CUDA)

# ── Document parsing ───────────────────────────────────────────────────────────
unstructured[all-docs]    # PDF, DOCX, HTML, MD parsing
pdfminer.six
docx2txt
markdown
beautifulsoup4

# ── Object storage ────────────────────────────────────────────────────────────
boto3                     # S3 upload/download

# ── Async task queue ──────────────────────────────────────────────────────────
celery[rabbitmq]          # async worker
# ── Testing ───────────────────────────────────────────────────────────────────
pytest
pytest-asyncio
pytest-timeout            # per-test timeout guard
httpx                     # async HTTP client for FastAPI test client
testcontainers[postgres,redis]  # real containers in regression tests
respx                     # httpx-level mock for OpenAI/Gemini HTTP calls
ragas                     # RAG evaluation metrics
datasets                  # HuggingFace datasets (used by RAGAS)

# ── Load testing ──────────────────────────────────────────────────────────────
locust
