[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
markers =
    slow: slow tests requiring a real Celery worker or LLM API calls
    integration: HTTP-layer tests spanning multiple tenants or users
addopts = -v --timeout=60 -m "not slow"
